{
	"name": "@video-editor/mock-vod",
	"version": "1.0.0",
	"description": "Mock VOD service emulating MPD-generate + DASH segments (port 5050)",
	"main": "src/index.ts",
	"scripts": {
		"dev": "node --watch --env-file=.env src/main.ts",
		"start": "node src/main.ts",
		"test": "vitest run",
		"type-check": "tsc --noEmit",
		"lint": "biome check . --write"
	},
	"type": "module",
	"dependencies": {
		"@fastify/cors": "^11.2.0",
		"fastify": "catalog:"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
