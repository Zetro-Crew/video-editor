{
	"name": "@video-editor/core-mock",
	"version": "1.0.0",
	"description": "Mock server for the core service API (port 8002)",
	"main": "src/index.ts",
	"scripts": {
		"dev": "node --watch src/index.ts",
		"start": "node src/index.ts",
		"type-check": "tsc --noEmit",
		"lint": "biome check . --write"
	},
	"type": "module",
	"dependencies": {
		"@fastify/cors": "^11.2.0",
		"fastify": "catalog:"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"typescript": "catalog:"
	}
}
