VITE_EDITOR_PARENT_ORIGINS=http://localhost:8080
# Production only — appended to window.location.origin behind the gateway:
VITE_CORE_EXTENSION=:8002/private
# Local dev — overrides VITE_CORE_EXTENSION with a full URL (avoids malformed localhost:3000:8002 URLs):
VITE_CORE_BASE_URL=http://localhost:8002/private
VITE_SERVER_EXTENSION=
